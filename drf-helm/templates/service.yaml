apiVersion: v1
kind: Service
metadata:
  name: {{ include "drf-helm.fullname" . }}
  labels:
    {{- include "drf-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drf-helm.selectorLabels" . | nindent 4 }}
